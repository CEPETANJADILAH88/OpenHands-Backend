# Complete requirements for Hugging Face Spaces
# Based on pyproject.toml but optimized for HF Spaces

# Core FastAPI and server dependencies
fastapi
uvicorn[standard]
python-multipart
python-dotenv

# LLM and AI dependencies
litellm>=1.60.0,!=1.64.4,!=1.67.*
aiohttp>=3.9.0,!=3.11.13
google-generativeai
anthropic
openai

# Core utilities
termcolor
toml
types-toml
numpy
json-repair
html2text
pexpect
jinja2>=3.1.3
tenacity>=8.5,<10.0
zope-interface==7.2
pathspec>=0.12.1
pyjwt>=2.9.0
dirhash
tornado
rapidfuzz>=3.9.0
whatthepatch>=1.0.6
protobuf>=5.0.0,<6.0.0

# Browser automation (required by browsing_agent)
browsergym-core==0.13.3

# Monitoring and telemetry
opentelemetry-api>=1.33.1
opentelemetry-exporter-otlp-proto-grpc>=1.33.1

# Terminal multiplexer (required by some agents)
libtmux>=0.37,<0.40

# GitHub integration
pygithub>=2.5.0

# Job processing
joblib

# WebSocket and SSE support
python-socketio>=5.11.4
sse-starlette>=2.1.3

# System utilities
psutil
python-json-logger>=3.2.1
prompt-toolkit>=3.0.50
anyio==4.9.0

# MCP support
fastmcp>=2.5.2

# Content processing
python-frontmatter>=1.1.0
PyPDF2
python-pptx
pylatexenc
python-docx
bashlex>=0.18

# Cloud storage and external services
boto3
redis>=5.2,<7.0

# Google APIs
google-api-python-client>=2.164.0
google-auth-httplib2
google-auth-oauthlib

# External services (lightweight)
e2b>=1.0.5,<1.6.0

# Removed problematic dependencies for HF Spaces:
# - docker (not needed in containerized environment)
# - pythonnet (Windows-specific, causes build issues)
# - modal (external cloud service, heavy)
# - daytona-sdk (not essential)
# - poetry (not needed at runtime)
# - qtconsole (GUI dependency, not needed)
# - ipywidgets (Jupyter dependency, not needed)
# - google-cloud-aiplatform (very heavy dependency)
# - minio (external service, not essential)
# - stripe (payment processing, not needed)
# - runloop-api-client (external service)
# - openhands-aci (Azure specific, not needed)